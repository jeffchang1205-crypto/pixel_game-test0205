# Pixel Quest - Cyberpunk Quiz Game 🕹️

這是一個使用 **React + Vite** 構建的 Premium 像素風格問答遊戲，使用 **Google Sheets** 作為資料庫，並透過 **Google Apps Script** 作為後端 API。

## 🌟 特色

- **賽博龐克像素美學**：霓虹發光、CRT 掃描線特效與復古 UI 設計。
- **動態題庫**：直接從您的 Google Sheet 抓取題目。
- **成績追蹤**：將使用者的分數、遊玩次數與通關狀態記錄回 Google Sheet。
- **Boss 戰鬥**：每一關皆配有隨機生成的像素風格 "Boss" 關主。

---

## 🚀 專案安裝

1.  **複製 (Clone) 或下載** 此專案。
2.  **安裝依賴套件**：
    ```bash
    npm install
    ```
3.  **啟動開發伺服器**：
    ```bash
    npm run dev
    ```
4.  打開瀏覽器並前往 `http://localhost:5173`。

---

## 📂 Google Sheet 設定

你需要建立一個 Google Sheet 來儲存題目與玩家的成績。

1.  建立一個 **新的 Google Sheet**。
2.  **工作表 1 名稱**：`題目` (Questions)
    - **標題列 (第一行)**：`ID`, `題目`, `A`, `B`, `C`, `D`, `解答`
    - **資料範例** (可以直接複製以下表格貼上)：

      | ID | 題目 | A | B | C | D | 解答 |
      |----|------|---|---|---|---|------|
      | 1 | GPT 中的 "G" 代表什麼？ | Great | Global | Generative | General | C |
      | 2 | ChatGPT 是哪一家公司開發的？ | Google | Microsoft | OpenAI | Meta | C |
      | 3 | AI 自信地產生錯誤資訊的現象稱為什麼？ | Hallucination (幻覺) | Dreaming (作夢) | Lie (說謊) | Bug (錯誤) | A |
      | 4 | 下列何者主要用於「圖片生成」？ | GPT-4 | Midjourney | Claude | Llama 3 | B |
      | 5 | LLM 的全名是什麼？ | Large Learning Model | Long Language Mode | Large Language Model | Logic Learning Machine | C |
      | 6 | 在生成式 AI 中，「Prompt」指的是什麼？ | 輸出結果 | 提示詞 (指令) | 程式碼編譯 | 模型權重 | B |
      | 7 | Transformer 架構的核心機制是什麼？ | Convolution | Pooling | Attention (注意力機制) | Recursion | C |
      | 8 | 下列哪一個模型屬於 Meta 開發的開源模型？ | Gemini | Claude | GPT-4 | Llama | D |
      | 9 | 為了讓模型更符合人類偏好，通常會使用什麼技術？ | SQL | RLHF | HTML | CSS | B |
      | 10 | RAG 技術的全名是？ | Really Awesome Generation | Retrieval-Augmented Generation | Random AI Generator | Robot Auto Guide | B |

3.  **工作表 2 名稱**：`回答` (Answers/Results)
    - **標題列 (第一行)**：`ID`, `闖關次數`, `總分`, `最高分`, `第一次通關分數`, `花了幾次通關`, `最近遊玩時間`
    - *當玩家完成遊戲時，系統會自動在此處新增資料。*

---

## 🔧 Google Apps Script (後端) 設定

這將連結您的 React App 與 Google Sheet。

1.  在您的 Google Sheet 中，點擊 **擴充功能 (Extensions)** > **Apps Script**。
2.  **刪除** `Code.gs` 文件中的所有現有程式碼。
3.  **複製** 本專案根目錄下 `google_apps_script.js` 檔案的完整內容。
4.  **貼上** 到 Apps Script 編輯器中。
5.  **儲存** 專案 (磁碟片圖示)。

### 📡 部署步驟 (關鍵！)

1.  點擊右上角的 **部署 (Deploy)** > **新增部署 (New deployment)**。
2.  **選取類型**：點擊齒輪圖示 ⚙️ > **網頁應用程式 (Web app)**。
3.  **設定 (Configuration)**：
    - **說明 (Description)**：`v1` (或任意名稱)。
    - **執行身分 (Execute as)**：`我 (您的 email)`。
    - **誰可以存取 (Who has access)**：**所有人 (Anyone)** (務必選擇此選項！)。
4.  點擊 **部署 (Deploy)**。
5.  **授權存取**：系統會要求您授予權限。
    - 點擊 `審查權限 (Review permissions)`。
    - 選擇您的帳戶。
    - 如果出現 "Google 尚未驗證此應用程式"，點擊 `進階 (Advanced)` > `前往 [專案名稱] (不安全)` (這是安全的，因為是您自己的程式碼)。
    - 點擊 `允許 (Allow)`。
6.  **複製** 最後畫面提供的 **網頁應用程式網址 (Web App URL)**。

---

## 🌍 環境變數設定

1.  在專案根目錄下，開啟 (或建立) `.env` 檔案。
2.  更新 `VITE_GOOGLE_APP_SCRIPT_URL` 變數，填入您剛剛複製的網址：

    ```env
    VITE_GOOGLE_APP_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
    VITE_PASS_THRESHOLD=3
    VITE_QUESTION_COUNT=5
    ```

3.  **重啟** npm 伺服器 (`Ctrl+C` 然後再次執行 `npm run dev`) 以讓變更生效。

---

## 🎮 如何遊玩

1.  在首頁輸入您的 **User ID**。
2.  點擊 **START** (插入投幣)。
3.  正確回答問題以擊敗 "Bosses"。
4.  在結算畫面查看您的分數！

---

## 🛠️ 疑難排解

- **CORS 錯誤 / 網路失敗**：
    - 確保您的 Web App 部署設定為 **"誰可以存取：所有人 (Anyone)"**。這是最常見的錯誤。
    - 如果您重新部署，請務必選擇 **新增部署 (New deployment)** (不要選管理現有部署)，以確保版本正確更新。開發時也可以使用 "測試部署 (Test deployment)" 的網址。
- **背景圖片遺失**：
    - 如果沒有圖片素材，專案會自動使用 CSS 漸層作為備案。
    - 若要使用自訂圖片，請將 `cyberpunk_bg_pixel.png` 放入 `src/assets/` 並取消 `src/components/Layout.jsx` 中的匯入註解。

---

## 📦 GitHub Pages 自動部署

本專案已包含 GitHub Actions 設定檔，可自動部署至 GitHub Pages。

### 設定步驟

1.  **建立 GitHub Repository**
    - 將此專案 Push 到 GitHub。

2.  **設定 Secrets (環境變數)**
    - 進入 GitHub Repository 的 **Settings** > **Secrets and variables** > **Actions**。
    - 點擊 **New repository secret**，依序新增以下變數 (值請參考您的 `.env` 檔)：
        - `VITE_GOOGLE_APP_SCRIPT_URL`: 您的 Google Apps Script Web App URL。
        - `VITE_PASS_THRESHOLD`: 例如 `3`。
        - `VITE_QUESTION_COUNT`: 例如 `5`。

3.  **開啟 GitHub Pages**
    - 進入 **Settings** > **Pages**。
    - 在 **Build and deployment** 下方的 **Source**，選擇 **GitHub Actions**。

4.  **觸發部署**
    - 只要 Push 程式碼到 `main` 分支，GitHub Actions 就會自動開始打包並部署。
    - 部署完成後，您可以在 GitHub Repository 的首頁右側或是 Actions 頁籤中看到您的網站連結！
